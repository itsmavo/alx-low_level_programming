#include "main.h"

/**
 * add - adds two number from input
 * @a - first parameter
 * @b - second parameter
 *
 * Description: adds two numbers
 * Return: Always (a + b)
 */
int add(int a, int b)
{
return (a + b);
}
